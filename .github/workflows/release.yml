name: Release

on:
  push:
    tags:
      - 'v*' # Запуск при пуше тега, например v0.1.0

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build Release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Archive Binary
        run: |
          cd target/${{ matrix.target }}/release
          tar -czf ../../../rustjack-${{ matrix.target }}.tar.gz rustjack
          cd ../../../

      - name: Upload Artifacts
        uses: softprops/action-gh-release@v1
        with:
          files: rustjack-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  update-tap:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Update Homebrew Tap
        uses: mislav/bump-homebrew-formula-action@v3
        with:
          # Ссылка на формулу в вашем репозитории tap
          formula-name: rustjack
          homebrew-tap: johncuba/homebrew-tap
          base-branch: main
          download-url: https://github.com/johncuba/rustjack/releases/download/${{ github.ref_name }}/rustjack-x86_64-apple-darwin.tar.gz
        env:
          COMMITTER_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}